{
  "name": "miss-google",
  "version": "1.0.0",
  "description": "Convert text to google voice",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build_win": "electron-packager . MissGoogle --platform=win32 --arch=x64 --asar --overwrite --extra-resource=./ffmpeg.exe"
  },
  "author": "Mike chen",
  "license": "ISC",
  "devDependencies": {
    "electron": "^11.1.1"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "electron-packager": "^15.2.0",
    "electron-root-path": "^1.0.16"
  }
}
